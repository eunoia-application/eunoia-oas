name: 🚀 Deploy Contracts

# Lockstep-релиз: весь contracts-репо едет под одной версией (parent pom <revision>).
# Деплоит parent + 4 Maven-модуля + npm api-types за раз. Версию бампать в pom.xml → <revision>.
on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      # ─────────────────────────── Maven (backend JARs) ───────────────────────────
      # Один root-деплой: реактор сам ставит shared-contract первым (он зависимость
      # остальных) и публикует parent pom (нужен потребителям для резолва дочерних).

      - name: Deploy Maven contracts (parent + 4 modules)
        run: |
          REVISION=$(mvn -q -N help:evaluate -Dexpression=revision -DforceStdout)
          echo "📦 Deploying Eunoia contracts v${REVISION} (lockstep)..."
          mvn -B clean deploy -DskipTests
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ──────────────────────── npm (frontend api-types) ──────────────────────────
      # Единый пакет @eunoia-application/api-types на весь REST. Версия деривится из
      # того же pom <revision> (build-types.mjs синхронизирует package.json).
      # Идемпотентно: если версия уже в registry — publish пропускается.

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@eunoia-application'

      - name: Publish frontend api-types (npm)
        run: |
          cd api-types
          npm install --no-audit --no-fund
          npm run build            # генерит dist/ + синхронит package.json version из pom <revision>
          VERSION=$(node -p "require('./package.json').version")
          if npm view "@eunoia-application/api-types@${VERSION}" version >/dev/null 2>&1; then
            echo "⏭️  @eunoia-application/api-types@${VERSION} уже опубликован — пропускаю (бампни <revision> для релиза)."
          else
            echo "📦 Publishing @eunoia-application/api-types@${VERSION}..."
            npm publish
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Success summary
        run: |
          REVISION=$(node -p "require('./api-types/package.json').version")
          echo "✅ DEPLOYMENT SUCCESSFUL — Eunoia contracts v${REVISION}"
          echo "👤 Triggered by: ${{ github.actor }}"
          echo "📅 $(date)"
          echo ""
          echo "📊 Packages: https://github.com/eunoia-application/eunoia-oas/packages"
          echo "🔧 Maven (backend): com.eunoia.application:{shared,notes,tags,auth}-contract:${REVISION}"
          echo "⚛️  npm (frontend):  @eunoia-application/api-types@${REVISION}"
